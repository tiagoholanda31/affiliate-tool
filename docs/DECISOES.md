# Decisões de arquitetura e produto

A versão canônica (em inglês, sanitizada para uso público) está em [`DECISIONS.md`](./DECISIONS.md).

Cada entrada segue o formato contexto ? decisão ? consequências. Novas decisões entram no fim, sem reescrever as antigas.

A spec original (`docs/spec/`) e as fatias (`docs/fatias/`) descrevem o produto em português. Quando stack e spec divergem, vale a decisão registrada (em especial **D-013**: Next 16 + Prisma 7).
