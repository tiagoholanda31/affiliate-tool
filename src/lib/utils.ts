/**
 * Junta classes condicionais resolvendo conflitos do Tailwind
 * (`px-2 px-4` → `px-4`).
 *
 * Reexportado do pacote `cn` (shadcn), que é o que os componentes de
 * `src/components/ui` usam — uma implementação só, para não divergirem.
 */
export { cn } from "cn";
